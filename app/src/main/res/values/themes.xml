<resources>
    <!-- Base application theme. -->
    <style name="Theme.GithubRepo" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/gray</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/gray</item>
        <!-- Customize your theme here. -->

        <item name="materialButtonStyle">@style/GlobalButtonStyle</item>
        <item name="toolbarStyle">@style/GlobalToolBarStyle</item>
        <item name="android:progressBarStyle">@style/CircularProgress</item>
    </style>

</resources>